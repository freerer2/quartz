```jsx
  //네비게이션

      let navigation = useNavigation();

      <div style={{ position: "fixed", top: 0, right: 0 }}>

        {navigation.state !== "idle" && <p>Navigation in progress...</p>}

      </div>
```

```jsx
//로더 함수
  return defer({
    event: await loadEvent(id),
    events: loadEvents(),
  });
```

```jsx
	라우팅
	{ 
	  path : '/',
	  element: <RootLayout />,
	  children: [
		{ 
		  path : '/',
		  async lazy() {
	          let { Posts, loader } = await import("./routes/Posts");
	          return {
		          Component: <Posts/>,
		          loader: loader
	          };
          },
	      children: [
		      { path : '/create-post', element: <NewPost />, action: dataAction },
		      { path : '/:id', 
			    async lazy() {
		            let { Details, loader } = await import("./routes/Details");
		            return { 
			            Component: Details,
			            loader: loader
		            };
	            }
	          }
		]}
	] }, 
```